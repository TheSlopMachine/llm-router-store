# llm-router-store

Official plugin store for [llm-router](https://github.com/TheSlopMachine/llm-router).
Single-file Lua provider plugins live in `llm-router-plugins/`.

Connect it in the dashboard: Plugins → Catalog → Add repository → GitHub
owner `TheSlopMachine`, repository `llm-router-store`.
